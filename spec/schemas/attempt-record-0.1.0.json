{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/open-gw/ctier/spec/schemas/attempt-record-0.1.0.json",
  "title": "ctier attempt record",
  "description": "The enforcement point refused without classifying, because the policy service was unavailable. Not a Decision with a null applied tier. This format reaches 1.0.0 when an implementation other than the reference one has been held to records in it. Until then it is normative — it defines what an Attempt is — but it has only ever been read by its author, which is a weaker claim than stability.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "ctierSchema",
    "id",
    "timestamp",
    "operationAnchor",
    "declaredTier",
    "appliedTier",
    "composition",
    "reason"
  ],
  "properties": {
    "ctierSchema": { "const": "attempt-record/0.1.0" },
    "id": { "type": "string", "minLength": 1 },
    "timestamp": { "type": "string", "minLength": 1 },
    "operationAnchor": { "type": "string", "minLength": 1 },
    "agentId": { "type": ["string", "null"] },
    "correlationScope": { "type": ["string", "null"] },
    "declaredTier": { "type": "integer", "minimum": 1, "maximum": 4 },
    "appliedTier": { "type": "null" },
    "composition": {
      "$ref": "ctier-extensions-0.1.0.json#/$defs/digest"
    },
    "reason": { "type": "string", "minLength": 1 }
  }
}
